= form_with(model: @post, local: true) do |f|
  - if @post.errors.any?
    - @post.errors.full_messages.each do |msg|
      .error_msg = msg
      br
  .field
    = f.label :images, class:"post-label"
    = f.file_field :images, multiple: true, class:"post-item__file"
    = f.file_field :images, multiple: true, class:"post-item__file"
    = f.file_field :images, multiple: true, class:"post-item__file"
  br
  .field
    = f.label :content, class:"post-label"
    = f.text_area :content, class:"post-item"
  br
  .actions
    = f.submit "post", class:"btn btn-success"
