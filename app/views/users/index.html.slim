.container
  ul.user-list
    - @users.each do |user|
      li.user-item = user.email
      - if user.admin_flag == false
        = link_to "管理者へ変更", edit_user_path(user.id)
      - else
        = link_to "一般へ変更", edit_user_path(user.id)
      = link_to "delete", user_path(user.id), method: :delete, data: {confirm: "OK?"}
